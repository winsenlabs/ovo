# W12: Management API, versions and config validation

Status: **in progress**

Dependencies: W02, W03. Baseline: 5–8 engineer-days. Owner: OVO implementation team.

## Tasks and exit checks

- [ ] Implement drafts/releases, optimistic edits, validation, publication and rollback.
- [ ] Add call/config/search APIs, signed business webhooks, command status and event cursors.
- [ ] Generate OpenAPI/client; enforce existing workspace/role boundaries without expanding into a separate IAM product.
- [ ] Exit: frontend can manage all required agent settings through APIs; immutable releases and stale-edit conflict tested.

## Acceptance links

A17, A18, A22, A24, A49, A52

## Evidence

Persisted API, workspace roles, async optimistic drafts, atomic immutable releases, transactional provider-binding and MCP connection/approval/schema snapshots, and a production PostgreSQL control adapter are implemented. Collection reads are bounded. Self-hosted encrypted-store credentials and explicit trusted-proxy configuration are covered. An explicit rollback operation, signed business webhooks, and generated OpenAPI/client work remain open, so the work package stays in progress.

[Foundation evidence](../../docs/evidence/control-implementation.md) and [production storage evidence](../../docs/evidence/production-control-storage.md). Real disposable PostgreSQL tests cover migrations, concurrency, rollbacks and each persisted control domain. No full work-package exit gate is certified. See [progress](../../docs/progress.md).

## Next action

Finish the explicit rollback operation, signed business webhooks and generated OpenAPI/client coverage, then verify the frontend exit gate. Keep live integration claims separate from simulations.
